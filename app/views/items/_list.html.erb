<ul class="itemlist">
  <% list.each do |item| %>
  <li <%= "class=archived" if item.archive %>>
    <div class="item"><%= link_to item.summary, edit_item_path(item) %></div>
  </li>
  <% end %>
</ul>
<br />
